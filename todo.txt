https://www.postman.com/nguyenkhoiteam/api-th-gii-s-api-tgs-com/collection/kq7lmbu/vn-public-apis

- Legends:
  🔥 Working on.
  ✅ Completed.
  ❌ Not started.
  📝 Some notes.

- 🦾 Techs used:
  - React for the frontend.
  - Node.js with Express for the backend.
  - MongoDB for the database.
  - Firebase for image storage and authentication.
  - Mailtrap/Nodemailer for email sending services.
  - Twilio for SMS sending services.
  - Auto generate JWT secret key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
  - FrontEnd:
    - popperjs for toasting.
    - font-awesome for icons.
    - react-router-dom for routing.
    - zustand for controllers.
    - react-google-maps/api for location.
    - lodash.debounce for geocoding (geographic coordinates).
    - jspdf, html2canvas for generating PDF file with content.
  - Faker for mocking data.

- 📝 Todos:
  - Frontend:
    - When user not verified not login -> redirect to verify page ✅.
    - Find default image for user profile ✅.
    - Find default image for product ❌.
    - Auto login user when user is verified ✅.
    - When user auth by Google send back to BackEnd idToken ✅.

  - Backend - API: (remember isDeleted field)
    - Because __v field is only activated with doc.save() so stick with find-modify-save method when modifying data 📝.
    - Sending welcome email after user is verified if they have email ✅.
    - Dev auth API ✅.
    - Dev user API ✅.
    - Dev roles & permissions API ✅.
    - Dev product API ✅.
    - Dev GRN API ✅.
    - Template for email sending ✅.
    - Better user friendly when sending email or SMS (with datetime for lock/unlock) ✅.
    - What happen when user add last items to their cart -> checkout -> leave checkout -> return to their cart (product will be display as out of stock temporary) ❌.
    - Email user when they've leaving their pending cart for to long before removing them ❌.
    - Refresh token ✅.

  - Database init:
    - Init roles & permissions ✅.
    - Mock data ✅.

  - Features:
    - Dynamic default avatar for user profile ❌.
    - Resend verification code feature ❌.
    - Storing end-user search keywords for more features about it (eg: trending searches, related searches) ❌.

- 📜 Rules:
  - FrontEnd:

  - BackEnd:
    - Requests: Optional fields can be undefined or null.
    - Responses: Optional fields are always present (with value or null), never undefined.
    - Models: Optional fields use default: null or default: [] (never missing in DB).