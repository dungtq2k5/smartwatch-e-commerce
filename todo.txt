- VN addresses: https://www.postman.com/nguyenkhoiteam/api-th-gii-s-api-tgs-com/collection/kq7lmbu/vn-public-apis

- Legends:
  🔥 Working on.
  ✅ Completed.
  ❌ Not started.
  📝 Some notes.

- 🦾 Techs used:
  - React for the frontend.
  - Node.js with Express for the backend.
  - MongoDB for the database.
  - Firebase for image storage and authentication.
  - Mailtrap/Nodemailer for email sending services.
  - Twilio for SMS sending services.
  - Auto generate JWT secret key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
  - Faker for mocking data.
  - multer, exceljs for handling multipart/form-data (file uploads).
  - libphonenumber-js, i18n-iso-countries for handling telephone input.
  - postal-codes-js for handling postal code validation.

  - FrontEnd:
    - popperjs for toasting.
    - font-awesome for icons.
    - react-router-dom for routing.
    - zustand for controllers.
    - react-google-maps/api for location.
    - lodash.debounce for geocoding (geographic coordinates).
    - jspdf, html2canvas for generating PDF file with content.
    - exceljs for handling Excel files.
    - react-phone-number-input for telephone input.
    - html5-qrcode for scanning code.

- 📝 Todos:
  - Frontend:
    - When user not verified not login -> redirect to verify page ✅.
    - Find default image for user profile ✅.
    - Find default image for product ❌.
    - Auto login user when user is verified ✅.
    - When user auth by Google send back to BackEnd idToken ✅.
    - Only display create user/product/... if users have permission ❌.
    - Ctrl+K to focus search input box ❌.
    - Create a button component with isLoading as props ❌.
    - Better ApiErr UI component to display error message and status code from API ❌.
    - export FormData type from create component to reuse in edit component ❌.
    - Use lazy import of needed ❌.
    - Create Button component with isLoading props ✅.
    - Update all related user components to display 'createdBy' field ❌.
    - Assign default err to undefined instead of empty string ❌.
    - Update err type to undefined for select input ✅.
    - For telephone input use inputMode="numeric" ✅.
    - Optimize repetitive code in management page (eg: user, product, order, return, .etc) with a generic component and pass props to customize it for each case ❌.

  - Backend - API: (remember isDeleted field)
    - Because __v field is only activated with doc.save() so stick with find-modify-save method when modifying data 📝.
    - Sending welcome email after user is verified if they have email ✅.
    - Dev auth API ✅.
    - Dev user API ✅.
    - Dev roles & permissions API ✅.
    - Dev product API ✅.
    - Dev GRN API ✅.
    - Template for email sending ✅.
    - Better user friendly when sending email or SMS (with datetime for lock/unlock) ✅.
    - What happen when user add last items to their cart -> checkout -> leave checkout -> return to their cart (product will be display as out of stock temporary) ❌.
    - Email user when they've leaving their pending cart for to long before removing them ❌.
    - Refresh token ✅.
    - Also save import GRN file to db ❌.
    - Use 'joi' or 'express-validator' for validating request body ❌.
    - Add 'createdBy' field for user schema (also handle chicken or egg first problem) ❌.
    - Create a dedicated postman collection for API testing ❌.
    - Create a dedicate postman collection for API testing and attach to README ❌.
    - Optimize get all brand/category/os endpoints with lighter version ❌.
    - Create alias mapping field name for importation endpoints ❌.
    - Create user details endpoint also get history of their orders, total spending, .etc ❌.
    - Follow security best practices for your backend: https://www.freecodecamp.org/news/how-to-harden-your-nodejs-apis-security-best-practices/
    - If data states like 'refundStates, returnStates,...' rarely change, you can add a **Cache-Control header** to the response. This tells the admin's browser to keep the data for, say, 1 hour, so they don't even hit your server when they navigate back and forth.
      ```typescript
      res.set('Cache-Control', 'public, max-age=3600'); // 1 hour
      ```

  - Database init:
    - Init roles & permissions ✅.
    - Mock data ✅.

  - Features:
    - Dynamic default avatar for user profile ❌.
    - Resend verification code feature ❌.
    - Storing end-user search keywords for more features about it (eg: trending searches, related searches) ❌.

  - DevOps:
    - Add License file for the project ❌.
    - Add CHANGELOG file for the project ❌.

- 📜 Rules:
  - FrontEnd:

  - BackEnd:
    - Requests: Optional fields can be undefined or null.
    - Responses: Optional fields are always present (with value or null), never undefined.
    - Models: Optional fields use default: null or default: [] (never missing in DB).